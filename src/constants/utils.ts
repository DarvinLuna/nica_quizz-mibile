import { hs, ms } from '../utils';

export const ToastType: Record<string, string> = {
  SUCCESS: 'success',
  ERROR: 'error',
  INFO: 'info',
};

export const SCREEN_PADDING_HORIZONTAL = hs(20);
export const BORDER_RADIUS = ms(8);
